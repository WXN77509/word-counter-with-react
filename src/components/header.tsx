function Header() {
  return (
    <header className="w-full max-w-[1240px] px-4 flex items-center flex-col gap-2">
      <h1 className="text-3xl font-bold text-reder">Word Counter</h1>
      <p className="text-center max-w-[750px]">
        Track word and character count, fix grammar, summarize text, extract
        keywords and more for any text with this advanced free tool.
      </p>
    </header>
  );
}

export default Header;
